import React from 'react';

export function CustomTooltip(payload?: any) {
    console.log('payload', payload);
    // if (payload.length === 0 || payload[0].payload.label === 'IGNORE') return null;
    // return payload[0].payload.label;
    return <div>AAA</div>;
}
